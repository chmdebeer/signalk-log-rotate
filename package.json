{
  "name": "signalk-log-rotate",
  "version": "0.1.2",
  "description": "A Signalk plugin to rotate data source logs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "signalk-node-server-plugin"
  ],
  "dependencies": {
    "node-cron": "latest"
  }
}
